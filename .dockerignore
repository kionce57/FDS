# ===========================================
# FDS Docker Ignore File
# 優化目標: 最小化 build context 加速建置
# ===========================================

# Python bytecode and caches
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments (使用 container 內的 .venv)
.venv/
venv/
ENV/
env/

# Development and testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/
# tests/ is mounted as volume, exclude the large video files from build context
tests/fixtures/videos/*.mp4

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Git
.git/
.gitignore
.gitattributes

# Data and logs (mounted as volumes)
data/
logs/
*.db
*.db-shm
*.db-wal

# Documentation (不需要在 container 內)
docs/

# CI/CD
.github/
.claude/

# Docker files (避免遞迴)
Dockerfile
docker-compose*.yml
.dockerignore

# Temporary files
tmp/
temp/
*.tmp
.DS_Store

# Environment files (敏感資訊)
.env
.env.*
!.env.example

# Large model files (如果已經在 container 內就不需要重複複製)
# 注意: yolov8n-pose.pt 需要被複製，所以不要排除

# Lock files for other package managers
package-lock.json
yarn.lock
